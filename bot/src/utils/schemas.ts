// ─── Thin re-export — source of truth is @memeover/shared ─────────────────────
export {
	type ClientMessage,
	ClientMessageSchema,
	type JoinMessage,
	type LeaveMessage,
	type PongMessage,
} from "@memeover/shared";

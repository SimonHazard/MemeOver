// ─── Thin re-export — source of truth is @memeover/shared ─────────────────────
export {
	type ErrorMessage,
	type JoinAckMessage,
	type MediaEvent,
	type MediaType,
	MediaTypeSchema,
	type PingMessage,
	type ServerMessage,
	ServerMessageSchema,
	type TextEvent,
} from "@memeover/shared";
